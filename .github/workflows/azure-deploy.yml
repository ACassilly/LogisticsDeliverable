name: Azure VM Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  ssh-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Sync managed env block to VM
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
          SSH_KEY_B64: ${{ secrets.SSH_KEY_B64 }}
          ENV_PRODUCTION_MANAGED: ${{ secrets.ENV_PRODUCTION_MANAGED }}
        run: |
          if [ -z "$ENV_PRODUCTION_MANAGED" ]; then
            echo "ENV_PRODUCTION_MANAGED secret not set; skipping env sync."
            exit 0
          fi
          mkdir -p ~/.ssh
          echo "$SSH_KEY_B64" | base64 -d > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null
          printf '%s\n' "$ENV_PRODUCTION_MANAGED" > /tmp/managed.env
          scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no /tmp/managed.env "$SSH_USERNAME@$SSH_HOST:/tmp/managed.env"
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "$SSH_USERNAME@$SSH_HOST" << 'ENVSYNC'
          set -e
          cd /opt/LogisticsDeliverable
          touch .env.production
          awk 'BEGIN{skip=0} /^# >>> riven-managed >>>$/{skip=1;next} /^# <<< riven-managed <<<$/{skip=0;next} skip==0{print}' .env.production > /tmp/env.base
          { cat /tmp/env.base; echo "# >>> riven-managed >>>"; cat /tmp/managed.env; echo "# <<< riven-managed <<<"; } > .env.production
          rm -f /tmp/env.base /tmp/managed.env
          echo "Managed env block synced into .env.production"
          ENVSYNC

      - name: Deploy to Azure VM via SSH
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
          SSH_KEY_B64: ${{ secrets.SSH_KEY_B64 }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY_B64" | base64 -d > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "$SSH_USERNAME@$SSH_HOST" << 'DEPLOY'
          set -e
          cd /opt/LogisticsDeliverable
          git fetch origin main && git reset --hard origin/main
          # Build the pattern dynamically so this YAML file never contains the literal bg-[url(...)] class
          # that Tailwind v4's content scanner would otherwise pick up
          ASSET="pattern-bg.svg"
          PATTERN="bg-\\[url('/images/hero/$ASSET')\\]"
          grep -rl "$PATTERN" src 2>/dev/null | xargs -r sed -i "s|$PATTERN||g" || true
          export CI=true
          corepack enable || sudo corepack enable
          corepack prepare pnpm@latest --activate || sudo corepack prepare pnpm@latest --activate
          pnpm install --no-frozen-lockfile
          rm -rf .next node_modules/.cache
          NEXT_PUBLIC_APP_URL=https://portlandialogistics.com pnpm build
          sudo pkill -f "PM2|pm2\b" 2>/dev/null || true
          sudo fuser -k 3000/tcp 2>/dev/null || true
          sleep 2
          pm2 kill 2>/dev/null || true
          pm2 start node_modules/next/dist/bin/next --name portlandia-logistics -- start -p 3000
          pm2 save
          sleep 3
          curl --fail --silent --show-error http://localhost:3000 >/dev/null
          curl --fail --silent --show-error -o /dev/null http://localhost:3000/images/services/ltl-hero.png
          echo "Deploy successful!"
          DEPLOY
